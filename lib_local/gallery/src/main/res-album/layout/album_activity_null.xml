<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Album.MatchParent"
    android:background="@color/albumBackground">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        style="@style/Gallery.Toolbar"
        tools:background="@color/galleryColorPrimary"
        tools:navigationIcon="@mipmap/gallery_ic_back">

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:ellipsize="end"
            android:lines="1"
            android:textSize="@dimen/textSize18"
            android:textStyle="bold"
            tools:text="@string/gallery_album_title"
            tools:textColor="@color/galleryFontLight" />

    </androidx.appcompat.widget.Toolbar>

    <LinearLayout
        style="@style/Album.MatchParent"
        android:layout_below="@+id/toolbar"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_message"
            style="@style/Album.WrapContent"
            android:textColor="@color/albumFontHint"
            android:textSize="@dimen/textSize12" />

        <Button
            android:id="@+id/btn_camera_image"
            style="@style/Album.Button.WrapContent"
            android:layout_marginTop="@dimen/gallery_dp_15"
            android:drawableStart="@mipmap/album_ic_image_camera"
            android:drawablePadding="@dimen/gallery_dp_6"
            android:gravity="center"
            android:text="@string/album_camera_image_capture"
            tools:textColor="@color/galleryFontLight" />

        <Button
            android:id="@+id/btn_camera_video"
            style="@style/Album.Button.WrapContent"
            android:layout_marginTop="@dimen/gallery_dp_15"
            android:drawableStart="@mipmap/album_ic_video_camera"
            android:drawablePadding="@dimen/gallery_dp_6"
            android:gravity="center"
            android:text="@string/album_camera_video_capture"
            tools:textColor="@color/galleryFontLight" />

    </LinearLayout>

</RelativeLayout>