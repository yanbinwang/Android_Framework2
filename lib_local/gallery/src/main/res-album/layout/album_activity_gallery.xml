<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/albumGalleryBackground"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        style="@style/Gallery.Toolbar"
        tools:background="@color/bgMain"
        tools:navigationIcon="@mipmap/gallery_ic_back" />

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="0pt"
        android:layout_weight="1"
        android:overScrollMode="never"
        android:scrollbars="none" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="34pt">

        <RelativeLayout
            android:id="@+id/layout_menu"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/albumMenuBackground"
            android:paddingHorizontal="10pt">

            <TextView
                android:id="@+id/tv_duration"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:drawablePadding="2pt"
                android:gravity="center"
                android:maxLines="1"
                android:textColor="@color/galleryFontLight"
                android:textSize="@dimen/textSize12"
                android:visibility="gone"
                app:drawableStartCompat="@mipmap/album_tag_video" />

            <CheckBox
                android:id="@+id/check_box"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:text="@string/album_check"
                android:textColor="@color/galleryFontLight"
                android:textSize="@dimen/textSize12" />

        </RelativeLayout>

        <FrameLayout
            android:id="@+id/layout_layer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/albumLayerDisabled">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="@string/album_item_unavailable"
                android:textColor="@color/galleryFontLight"
                android:textSize="@dimen/textSize14" />

        </FrameLayout>

    </FrameLayout>

</LinearLayout>